# このリポジトリの設定ファイル（例）です。
# 行は KEY=VALUE 形式です。空行と # で始まる行は無視されます。
# 値は他のキーを参照できます:
# - キー名を値にする形式: EXTRACT_INPUT_DIR=INPUT_DIR
# - ${KEY} 形式: EXTRACT_INPUT_DIR=${INPUT_DIR}
#
# INPUT_DIR:
#   役割: 画像フォルダ（サブフォルダ群）を置く親フォルダ（画像→PDFの入力）。
#   使用: src\folder2pdf.py（01_images_to_pdf.bat / 01_run_folder2pdf.ps1）
INPUT_DIR=01_images_input
#
# OUTPUT_DIR:
#   役割: 画像→PDFの出力フォルダ。
#   使用: src\folder2pdf.py / src\move_matched_pdfs.py
OUTPUT_DIR=01_pdf_output
#
# FOLDER2PDF_DEFAULT_METHOD:
#   役割: 01_images_to_pdf.bat のデフォルト方式。
#   値: img2pdf（推奨/デフォルト） / carrier
FOLDER2PDF_DEFAULT_METHOD=img2pdf
#
# FOLDER2PDF_OVERWRITE:
#   役割: 既存PDFを上書きするか（true/false）。
FOLDER2PDF_OVERWRITE=false
#
# FOLDER2PDF_DPI:
#   役割: img2pdf方式でPDF上の物理サイズ計算に使うDPI（デフォルト）。
FOLDER2PDF_DPI=300
#
# FOLDER2PDF_OPTIMIZE_MODE:
#   役割: img2pdf方式のサイズ/画質モード（デフォルト）。
#   値: lossless / auto / size
FOLDER2PDF_OPTIMIZE_MODE=auto
#
# FOLDER2PDF_JPEG_QUALITY:
#   役割: 再エンコード時のJPEG品質（auto/size で必要な場合）。
FOLDER2PDF_JPEG_QUALITY=90
#
# FOLDER2PDF_MAX_LONG_EDGE:
#   役割: 再エンコード時の最大長辺px（0=自動）。
FOLDER2PDF_MAX_LONG_EDGE=0
#
# FOLDER2PDF_AUTO_REENCODE_THRESHOLD_MB:
#   役割: autoモード時、PNGのファイルサイズが閾値(MB)を超えた場合のみ再エンコード。
FOLDER2PDF_AUTO_REENCODE_THRESHOLD_MB=6.0
#
# BASE_PDF_PATH:
#   役割: Carrier方式（テンプレPDF差し替え）で使うテンプレPDF。
#   例: BASE_PDF_PATH=C:\path\to\template.pdf
#BASE_PDF_PATH=
#
# SPLIT_OUTPUT_DIR:
#   役割: PDF分割結果の出力フォルダ。
#   使用: src\split_pdf.py（03_pdf_split.bat）
SPLIT_OUTPUT_DIR=03_split_pdfs
#
# SPLIT_OCR_FOLDER_ROOT:
#   役割: PDF分割完了後に「OCR用フォルダ（<元PDF名>/）」を作成する親フォルダ。
#         空欄のままならフォルダ作成はスキップします。
#   例: SPLIT_OCR_FOLDER_ROOT=${OCR_DIR}
SPLIT_OCR_FOLDER_ROOT=
#
# MERGE_OUTPUT_DIR:
#   役割: PDF結合結果の出力フォルダ。
#   使用: src\merge_pdf.py（04_pdf_merge.bat）
MERGE_OUTPUT_DIR=04_merged_pdfs
#
# OCR_DIR:
#   役割: OCRソフトが出力したPDFの置き場（照合/抽出/分割後のフォルダ作成で利用）。
#   使用: src\move_matched_pdfs.py 等
#   注: 現状、照合は OCR_DIR 直下のPDFのみ（サブフォルダは見ません）。
OCR_DIR=
#
# MATCHED_DIR:
#   役割: OCR済みと一致したPDFの移動先（無ければ作成）。
#   使用: src\move_matched_pdfs.py
MATCHED_DIR=02_matched_pdfs
#
# EXTRACT_INPUT_DIR:
#   役割: 文字抽出対象のPDFがあるフォルダ（テキストレイヤー付きPDF想定）。
#   使用: src\extract_pdf_text.py
EXTRACT_INPUT_DIR=${OCR_DIR}
#
# EXTRACT_OUTPUT_DIR:
#   役割: 文字抽出結果（.txt）の出力フォルダ。
#   使用: src\extract_pdf_text.py
EXTRACT_OUTPUT_DIR=05_extracted_texts
